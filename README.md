# Da Nang Real Estate Analytics

A self-hosted real estate analytics platform for the Da Nang market.

## Setup

Use `uv` to install dependencies and run CLI commands:

```bash
# Install dependencies
uv sync

# Run the CLI
uv run danang-realestate --help
```
